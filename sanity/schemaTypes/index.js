import {categorySchema} from './categorySchema'
import {productSchema} from './productSchema'
import {reviewSchema} from './reviewSchema'

export const schemaTypes = [productSchema, categorySchema, reviewSchema]
