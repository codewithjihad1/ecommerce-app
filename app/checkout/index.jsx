import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function Checkout() {
  return (
    <SafeAreaView>
      <Text>Welcome to the checkout</Text>
    </SafeAreaView>
  );
}
